# ~/.agents AI 讀者書寫規範（11 條）

> 讀者是 AI 模型，不是人。原則只有一個：**每一句話都要能被機械驗證或機械執行**——模型換代後散文語感會漂移，但「檔案存在」「grep 命中」「exit code」不會。`bin/agents-sync` 的 lint 據本檔執行；違反 = build fail，不是警告。

## 1. 檔頭 metadata（HTML comment，不佔注入 token 預算）

每個 `~/.agents/` 常駐檔第一行必為：`<!-- tier: 0 | consumed-by: claude,codex,copilot | generated-from: core/tier0-safety.md | last-verified: 2026-07-08 -->`。生成部署目標檔（`dist/`、`~/.codex/AGENTS.md` 等）另加 banner：`<!-- GENERATED by ~/.agents/bin/agents-sync @<git-sha> hash:<sha256 前 8 碼> — 勿手改；改 ~/.agents/core|hosts|rules -->`。手改生成檔 = bug；no-clobber 拒覆蓋無 banner 的檔並報錯。

## 2. 規則五要素格式

每條規則單行自足：`[T0-3] MUST NOT force-push 到 main/master。觸發：git push --force* 且目標分支為 main|master。例外：無。驗證：rules lint + hook audit-bash。` 五要素 = 穩定 ID + RFC2119 動詞（MUST / MUST NOT / SHOULD）+ 機械觸發條件 + 明示例外（沒有就寫「無」）+ 驗證方式。缺任一要素不得進 tier 檔。

## 3. 規則 ID 永不重編、永不回收

刪除或降級規則時留殼標記：`[T1-7 DEPRECATED→T0-2 2026-07]`。理由：舊 session transcript、commit message、cookbook 條目可能引用舊 ID；重編會讓歷史引用全部斷鏈且無人察覺。

## 4. 跨檔引用只用規則 ID 或檔路徑

禁止散文錨點（「見上方 merge gate 一節」「如前述」）。散文錨點在檔案被生成器重組後靜默失效；規則 ID 與路徑可被 lint 驗證。（實證：DCT repo doc-rot 審計中錨點解析器掃不到散文符號殘留，須另跑 grep 雙軌。）

## 5. 載入宣稱必附驗證指令

任何「X 會載入 Y」「X 繼承 Y」的句子，同段必附可執行探針指令 + 預期輸出。無法驗證的宣稱必須加 `UNVERIFIED:` 前綴且禁用肯定句式。（實證：Copilot「已自動載入 ~/.claude/CLAUDE.md」一句未驗證肯定句，讓完整 Hard Rules 失守一個月，見 proposals/01 F1。）

## 6. Fingerprint codeword

每個常駐注入檔含恰一句唯一指紋，格式 `FP:<檔名代號>-<年季>`，例 `FP:AGENTS-T0-2026Q3`。用途：context 級載入驗證。檔案級驗證（symlink 存在、hash 相符）不足以證明「進了 context」——兩起事故皆是檔案層正常、context 層死透。

## 7. 名稱引用必過 lint

skill / agent / MCP server / hook 名稱一律 lint 對照實體目錄；手寫清單禁止，索引只准機械生成。反引號引用的 skill / command 名必須存在於 `skills/` 或明標 `host:<name>-only` 豁免。（實證：Copilot 9 個死 skill 引用、`chrome-devtoolss` 拼錯，全是手寫清單腐爛。）

## 8. Context 硬預算

tier0 ≤30 行、routing ≤30 行、三家常駐生成總量 ≤10KB；超標 = agents-sync build fail。理由：Codex override 事故根因之一是「32KB 太長→有人另立短檔→短檔整檔取代長檔」。預算是防止下一次 override 誕生的結構性手段。

## 9. why 一句話 + 出處座標；可機械化者必須下沉

規則的 why 限一句 + 座標（commit SHA / PR 編號 / 審計日期），詳細敘事放 cookbook 或 proposal 附件。凡能寫成 hook / lint / test 的檢查，必須下沉為機械守護並從 prose 移除——prose 只留判斷類知識。

## 10. 語言與句式

zh-TW；術語照附錄 A 用詞對照表（建立／物件／佇列；禁「創建／質量／視圖」）；規則句禁隱喻、禁反問；每句單獨抽出仍可執行（模型可能只看到被 grep 出來的單句）。技術名詞與識別字保留原文。

## 11. 版控取代 .bak（`~/.agents` git 化後生效）

三家設定目錄（`~/.claude`、`~/.codex`、`~/.copilot`）的手工 `.bak` 慣例廢止：設定變更一律走 git commit；MUST NOT 再產生 `*.bak*`。既有 .bak 掃 secret 後刪除或歸檔 `attic/`。（實證 2026-07-07：`~/.codex` 累積 .bak，其中 3 份 `config.toml.bak` 額外複製了同一明文 API key——備份檔是 secrets 殘留的最大死角。）驗證：`ls ~/.claude/*.bak* ~/.codex/*.bak* ~/.copilot/*.bak* 2>/dev/null | wc -l` = 0。例外：app 自動生成的 runtime state 備份（如 `.codex-global-state.json.bak`，dotfile 開頭、app 自管生命週期）不算違規，不得手動刪除。

## 12. Claude 常駐面預算（CLAUDE.md + core 合計）

`~/.claude/CLAUDE.md` 與三個 `@import` 的 core 檔合計 ≤20KB（量測：`cat ~/.claude/CLAUDE.md ~/.agents/core/tier{0,1,2}-*.md | wc -c`）。觸發：編輯 CLAUDE.md 或 core。理由：2026-07-08 審計（F7）發現 CLAUDE.md 與 tier0-2 逐句重複 ~6KB——重複不只費 token，更製造 drift 面（改正本忘改副本）。CLAUDE.md 只放 Claude 專屬語意；與 tier 規則重疊者一律刪除改 rule-ID 引用。例外：無。驗證：量測式 ≤20480；`grep -c "原生（語言 / 框架" ~/.claude/CLAUDE.md` = 0（抽樣重複片語）。

## 13. 注入層加一刪一（≥90% 預算時生效）

任一 host 組裝體 ≥ 預算 90%（copilot 即 9216B）時，新增常駐行 MUST 在同批刪除等量重複 / 過時行補償，不得單向膨脹。觸發：`agents-sync --check` 顯示某 host ≥90%。理由：預算是防 override 的結構性手段（規則 8），逼近上限時「先加後刪」會讓 build fail 擋住合法修正。例外：安全紅線（tier0）新增可先入，同批補刪其他層。驗證：`--check` 通過且該 host 體積未淨增。


---

# 附錄（非注入常駐預算；供 AI on-demand 查閱，不進三家 host context）

> 由 tier2 遷入（原佔每家常駐預算 ~836B）。這兩份是「查閱型參考」非「每回合必駐規則」：用詞紅線本體 [T2-5] 已在 tier2 並指向附錄 A；Code Style（附錄 B）屬判斷類、無 tier 規則錨（不機械強制），完整對照與原則存此供 on-demand 查閱。

## 附錄 A：用詞對照（一律台灣慣用詞；T2-5 完整表）

create=建立、object=物件、queue=佇列、stack=堆疊、information=資訊、code=程式碼、running=執行、library=函式庫、building=建構、package=套件、class=類別、function=函式、memory=記憶體、built-in=內建、global=全域、example=範例、quality=品質、document=文件、file=檔案、data=資料、component=元件、integration=整合、search=搜尋、store=儲存

## 附錄 B：Code Style 判斷原則（app code；判斷類不機械強制，非本檔 11 條 lint 規範）

Favor pure functions；query 與 command 分離（CQS）｜Fail fast，不靜默吞錯｜DRY 第 3 次重複才抽（Rule of Three）｜YAGNI，不先建不必要抽象｜Composition > inheritance｜命名表 intent，註解解釋 why｜低耦合高內聚，外部依賴用 DI｜巢狀深度 ≤ 2–3 層，超過就抽方法。
